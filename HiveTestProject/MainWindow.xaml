<Window x:Class="HiveTestProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HiveTestProject.ViewModels"
        xmlns:views="clr-namespace:HiveTestProject.Views"
        mc:Ignorable="d"
        Title="Hive Dashboard"
        Height="600" Width="1050"
        MinHeight="500" MinWidth="900"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <!-- Main window border with rounded corners -->
    <Border CornerRadius="12" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a2e" Offset="0" />
                <GradientStop Color="#16213e" Offset="0.5" />
                <GradientStop Color="#0f3460" Offset="1" />
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════ -->
            <!-- Custom Title Bar                    -->
            <!-- ═══════════════════════════════════ -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- App Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                               Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBlock Text="Hive Dashboard" Style="{StaticResource HeaderText}" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>

                <!-- Window Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="&#xE921;" Style="{StaticResource TitleBarButton}" Click="Minimize_Click" ToolTip="Minimize" />
                    <Button Content="&#xE922;" Style="{StaticResource TitleBarButton}" Click="Maximize_Click" ToolTip="Maximize" x:Name="MaximizeButton" />
                    <Button Content="&#xE8BB;" Style="{StaticResource CloseButton}" Click="Close_Click" ToolTip="Close" />
                </StackPanel>
            </Grid>

            <!-- ═══════════════════════════════════ -->
            <!-- Dashboard Content                   -->
            <!-- ═══════════════════════════════════ -->
            <Grid Grid.Row="1" Margin="16,8,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1.4*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Clock Widget: top-left -->
                <views:ClockWidget Grid.Column="0" Grid.Row="0" Margin="4"
                                   DataContext="{Binding Clock}" />

                <!-- System Info Widget: top-center -->
                <views:SystemInfoWidget Grid.Column="1" Grid.Row="0" Margin="4"
                                        DataContext="{Binding SystemInfo}" />

                <!-- Notes Widget: right column, full height -->
                <views:NotesWidget Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Margin="4"
                                   DataContext="{Binding Notes}" />

                <!-- Calculator Widget: bottom-left -->
                <views:CalculatorWidget Grid.Column="0" Grid.Row="1" Margin="4"
                                        DataContext="{Binding Calculator}" />

                <!-- To-Do Widget: bottom-center -->
                <views:TodoWidget Grid.Column="1" Grid.Row="1" Margin="4"
                                  DataContext="{Binding Todo}" />
            </Grid>
        </Grid>
    </Border>
</Window>
